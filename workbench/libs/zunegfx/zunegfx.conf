##begin config
version 1.0
basename zunegfx
libbase ZuneGfxBase
libbasetype struct IntZuneGfxBase
options noautoinit
##end config

##begin cdef
#ifndef INTUITION_INTUITION_H
#   include <intuition/intuition.h>
#endif
#include <utility/tagitem.h>
#include <utility/hooks.h>
#include <graphics/gfx.h>
#include <graphics/rastport.h>
#include <graphics/view.h>

#include <libraries/zunegfx.h>

#ifndef ZuneGfxBase
extern struct Library *ZuneGfxBase;
#endif

struct LayerCompositor;
struct CompositorWindow;

/* Tag-based creation API - varargs convenience functions */
struct RenderContext *ZuneCreateRenderContext(Tag tag1, ...) __stackparm;
struct DrawingBoard *ZuneCreateDrawingBoard(Tag tag1, ...) __stackparm;
struct ZuneTexture *ZuneCreateTextureTags(Tag tag1, ...) __stackparm;
struct LayerCompositor *ZuneCreateLayerCompositorTags(Tag tag1, ...) __stackparm;
struct CompositorWindow *ZuneCompositorRegisterWindowTags(Tag tag1, ...) __stackparm;
##end cdef

##begin cdefprivate
#include "src/zunegfx_intern.h"
##end cdefprivate

##begin functionlist
# RenderContext Management (Window-centric API)
struct RenderContext *ZuneCreateRenderContextForWindow(struct Window *window, struct ColorMap *colormap, UWORD backend_type) (A0, A1, D0)
struct DrawingBoard *ZuneCreateDrawingBoardForRenderContext(struct RenderContext *rctx, UWORD width, UWORD height, ULONG flags) (A0, D0, D1, D2)
void ZuneDestroyRenderContext(struct RenderContext *rctx) (A0)
void ZuneClearRenderContext(struct RenderContext *rctx, ULONG color) (A0, D0)

# DrawingBoard Management
BOOL ZuneSetTarget(struct RenderContext *rctx, struct DrawingBoard *board) (A0, A1)
void ZuneDestroyDrawingBoard(struct RenderContext *rctx, struct DrawingBoard *board) (A0, A1)
void ZuneClearDrawingBoard(struct RenderContext *rctx, ULONG color) (A0, D0)

# Direct Pixel Access
APTR ZuneLockDrawingBoardPixels(struct RenderContext *rctx, ULONG *pitch) (A0, A1)
void ZuneUnlockDrawingBoardPixels(struct RenderContext *rctx) (A0)
ULONG ZuneGetPixel(struct RenderContext *rctx, struct ZunePoint *point) (A0, A1)
void ZuneSetPixel(struct RenderContext *rctx, struct ZunePoint *point, ULONG color) (A0, A1, D0)

# Performance and Batching
void ZuneBeginBatch(struct RenderContext *rctx) (A0)
void ZuneEndBatch(struct RenderContext *rctx) (A0)
void ZuneFlushBatch(struct RenderContext *rctx) (A0)
BOOL ZuneIsBatchingEnabled(struct RenderContext *rctx) (A0)
ULONG ZuneGetBatchCount(struct RenderContext *rctx) (A0)

# Blitting and Surface Operations
void ZuneBlit(struct RenderContext *src_rctx, struct RenderContext *dst_rctx, WORD src_x, WORD src_y, WORD dst_x, WORD dst_y, UWORD width, UWORD height) (A0, A1, D0, D1, D2, D3, D4, D5)
void ZunePresent(struct RenderContext *rctx, WORD src_x, WORD src_y, WORD dst_x, WORD dst_y, UWORD width, UWORD height) (A0, D0, D1, D2, D3, D4, D5)

# Clipping
BOOL ZuneSetClipRegion(struct RenderContext *rctx, struct Region *region) (A0, A1);
void ZuneClearClipRegion(struct RenderContext *rctx) (A0);
struct Region *ZuneCombineRegions(struct Region *r1, struct Region *r2, UBYTE operation) (A0, A1, D0);

# Color Utilities
ULONG ZuneRGBToColor(UBYTE r, UBYTE g, UBYTE b) (D0, D1, D2)
ULONG ZuneARGBToColor(UBYTE a, UBYTE r, UBYTE g, UBYTE b) (D0, D1, D2, D3)
ULONG ZuneBlendColors(ULONG color1, ULONG color2, UBYTE alpha) (D0, D1, D2)

# Core Drawing API - Basic filled shapes
void ZuneFillRectangle(struct RenderContext *rctx, struct ZuneRect *rect, const struct ZuneBrush *brush) (A0, A1, A2)
void ZuneFillRectangleRounded(struct RenderContext *rctx, struct ZuneRect *rect, UBYTE cornerRadius, const struct ZuneBrush *brush) (A0, A1, D0, A2)
void ZuneFillCircle(struct RenderContext *rctx, struct ZunePoint *center, UBYTE radius, const struct ZuneBrush *brush) (A0, A1, D0, A2)
void ZuneDrawLine(struct RenderContext *rctx, struct ZunePoint *start, struct ZunePoint *end, ULONG color) (A0, A1, A2, D0)

# Shape outlines
void ZuneDrawRectangleOutline(struct RenderContext *rctx, struct ZuneRect *rect, ULONG color) (A0, A1, D0)
void ZuneDrawRectangleRoundedOutline(struct RenderContext *rctx, struct ZuneRect *rect, UBYTE cornerRadius, ULONG color) (A0, A1, D0, D1)
void ZuneDrawCircleOutline(struct RenderContext *rctx, struct ZunePoint *center, UBYTE radius, ULONG color) (A0, A1, D0, D1)

# Styled versions
void ZuneDrawRectangleOutlineStyled(struct RenderContext *rctx, struct ZuneRect *rect, UBYTE lineWidth, ULONG color) (A0, A1, D0, D1)
void ZuneDrawCircleOutlineStyled(struct RenderContext *rctx, struct ZunePoint *center, UBYTE radius, UBYTE lineWidth, ULONG color) (A0, A1, D0, D1, D2)
void ZuneDrawRectangleRoundedOutlineStyled(struct RenderContext *rctx, struct ZuneRect *rect, UBYTE cornerRadius, UBYTE lineWidth, ULONG color) (A0, A1, D0, D1, D2)
void ZuneDrawLineStyled(struct RenderContext *rctx, struct ZunePoint *start, struct ZunePoint *end, UWORD width, ULONG color) (A0, A1, A2, D0, D1)

# Rounded rectangle with fill and border combined
void ZuneDrawRectangleRoundedStyled(struct RenderContext *rctx, struct ZuneRect *rect, UBYTE cornerRadius, UBYTE borderWidth, const struct ZuneBrush *fillBrush, ULONG borderColor) (A0, A1, D0, D1, A2, D2)

# Pixels
void ZuneDrawPixel(struct RenderContext *rctx, struct ZunePoint *point, ULONG color) (A0, A1, D0)

# Circles AA
void ZuneDrawCircleAA(struct RenderContext *rctx, struct ZunePoint *center, UBYTE radius, ULONG color) (A0, A1, D0, D1)
void ZuneFillCircleAA(struct RenderContext *rctx, struct ZunePoint *center, UWORD radius, const struct ZuneBrush *brush) (A0, A1, D0, A2)
void ZuneDrawCircleOutlineStyledAA(struct RenderContext *rctx, struct ZunePoint *center, UWORD radius, UBYTE borderWidth, ULONG color) (A0, A1, D0, D1, D2)
void ZuneFillCircleStyledAA(struct RenderContext *rctx, struct ZunePoint *center, UWORD radius, UBYTE borderWidth, const struct ZuneBrush *brush, ULONG borderColor) (A0, A1, D0, D1, A2, D2)

# AA Quality
void ZuneSetAntialiasingQuality(struct RenderContext *rctx, UBYTE quality) (A0, D0)
UBYTE ZuneGetAntialiasingQuality(struct RenderContext *rctx) (A0)

# Rounded rectangles AA
void ZuneFillRectangleRoundedAA(struct RenderContext *rctx, struct ZuneRect *rect, UBYTE cornerRadius, const struct ZuneBrush *brush) (A0, A1, D0, A2)
void ZuneFillRectangleRoundedStyledAA(struct RenderContext *rctx, struct ZuneRect *rect, UBYTE cornerRadius, UBYTE borderWidth, const struct ZuneBrush *brush, ULONG borderColor) (A0, A1, D0, D1, A2, D2)
void ZuneDrawRectangleRoundedOutlineStyledAA(struct RenderContext *rctx, struct ZuneRect *rect, UBYTE cornerRadius, UBYTE lineWidth, ULONG borderColor) (A0, A1, D0, D1, D2)
void ZuneDrawRectangleRoundedOutlineAA(struct RenderContext *rctx, struct ZuneRect *rect, UBYTE cornerRadius, ULONG color) (A0, A1, D0, D1)

# Lines AA
void ZuneDrawLineAA(struct RenderContext *rctx, struct ZunePoint *start, struct ZunePoint *end, ULONG color) (A0, A1, A2, D0)
void ZuneDrawLineStyledAA(struct RenderContext *rctx, struct ZunePoint *start, struct ZunePoint *end, UBYTE width, ULONG color) (A0, A1, A2, D0, D1)

# Texture Management
struct ZuneTexture *ZuneCreateTexture(struct RenderContext *rctx, UWORD width, UWORD height, UBYTE depth, ULONG format, ULONG flags) (A0, D0, D1, D2, D3, D4)
struct ZuneTexture *ZuneCreateTextureFromData(struct RenderContext *rctx, APTR data, UWORD width, UWORD height, UBYTE depth, ULONG format, ULONG pitch, ULONG flags) (A0, A1, D0, D1, D2, D3, D4, D5)
struct ZuneTexture *ZuneCreateTextureFromDrawingBoard(struct RenderContext *rctx, ULONG flags) (A0, D0)
struct ZuneTexture *ZuneCreateTextureFromDatatype(struct RenderContext *rctx, APTR dt_object, ULONG flags) (A0, A1, D0)
struct ZuneTexture *ZuneCreateTextureFromFile(struct RenderContext *rctx, CONST_STRPTR filename, struct Screen *screen, ULONG flags) (A0, A1, A2, D0)
void ZuneDestroyTexture(struct RenderContext *rctx, struct ZuneTexture *texture) (A0, A1)

# Texture Data Operations
BOOL ZuneUpdateTextureData(struct RenderContext *rctx, struct ZuneTexture *texture, APTR data, struct ZuneRect *rect) (A0, A1, A2, A3)
APTR ZuneLockTexturePixels(struct RenderContext *rctx, struct ZuneTexture *texture, ULONG *pitch) (A0, A1, A2)
void ZuneUnlockTexturePixels(struct RenderContext *rctx, struct ZuneTexture *texture) (A0, A1)
ULONG ZuneGetTexturePixel(struct RenderContext *rctx, struct ZuneTexture *texture, struct ZunePoint *point) (A0, A1, A2)
void ZuneSetTexturePixel(struct RenderContext *rctx, struct ZuneTexture *texture, struct ZunePoint *point, ULONG color) (A0, A1, A2, D0)

# Texture Rendering
void ZuneDrawTexture(struct RenderContext *rctx, struct ZuneTexture *texture, struct ZunePoint *position) (A0, A1, A2)
void ZuneDrawTextureScaled(struct RenderContext *rctx, struct ZuneTexture *texture, struct ZuneRect *dest_rect) (A0, A1, A2)
void ZuneDrawTextureRegion(struct RenderContext *rctx, struct ZuneTexture *texture, struct ZuneRect *src_rect, struct ZuneRect *dest_rect) (A0, A1, A2, A3)
void ZuneDrawTextureTinted(struct RenderContext *rctx, struct ZuneTexture *texture, struct ZunePoint *position, ULONG tint_color) (A0, A1, A2, D0)
void ZuneDrawTextureScaledTinted(struct RenderContext *rctx, struct ZuneTexture *texture, struct ZuneRect *dest_rect, ULONG tint_color) (A0, A1, A2, D0)
void ZuneDrawTextureRegionTinted(struct RenderContext *rctx, struct ZuneTexture *texture, struct ZuneRect *src_rect, struct ZuneRect *dest_rect, ULONG tint_color) (A0, A1, A2, A3, D0)
void ZuneDrawTextureTiled(struct RenderContext *rctx, struct ZuneTexture *texture, struct ZuneRect *dest_rect) (A0, A1, A2)

# Texture utilities
BOOL ZuneIsTextureValid(struct ZuneTexture *texture) (A0);

# Clipping utilities
struct Region *ZuneCreateCircleRegion(struct ZunePoint *center, WORD radius) (A0, D0);
struct Region *ZuneCreateRoundedRectRegion(struct ZuneRect *rect, WORD corner_radius) (A0, D0);

# Cache utilities
void ZuneInitPenCache(struct RenderContext *rctx, LONG *pens, UWORD count) (A0, A1, D0);

# Capture from RastPort to DrawingBoard (for reading background)
void ZuneCapture(struct RenderContext *rctx, struct RastPort *src_rp, WORD src_x, WORD src_y, WORD dst_x, WORD dst_y, UWORD width, UWORD height) (A0, A1, D0, D1, D2, D3, D4, D5)

# Sync FBO to bitmap (for mixed OpenGL/legacy drawing)
BOOL ZuneSync(struct RenderContext *rctx) (A0)

# Reload FBO from bitmap (after legacy drawing to bitmap)
BOOL ZuneReload(struct RenderContext *rctx) (A0)

# OpenGL context sharing support (for compositor integration)
APTR ZuneGetMasterGLContext() ()

# Layer Compositor API
struct LayerCompositor *ZuneCreateLayerCompositor(struct Screen *screen) (A0)
struct LayerCompositor *ZuneCreateLayerCompositorShared(struct Screen *screen, APTR masterGLContext) (A0, A1)
void ZuneDestroyLayerCompositor(struct LayerCompositor *comp) (A0)
BOOL ZuneActivateLayerCompositor(struct LayerCompositor *comp) (A0)
void ZuneDeactivateLayerCompositor(struct LayerCompositor *comp) (A0)
struct CompositorWindow *ZuneCompositorRegisterWindow(struct LayerCompositor *comp, struct Window *window, APTR glContext, struct DrawingBoard *board, UBYTE alpha) (A0, A1, A2, A3, D0)
void ZuneCompositorUnregisterWindow(struct LayerCompositor *comp, struct Window *window) (A0, A1)
void ZuneCompositorSetWindowAlpha(struct LayerCompositor *comp, struct Window *window, UBYTE alpha) (A0, A1, D0)
void ZuneCompositorMarkWindowDirty(struct LayerCompositor *comp, struct Window *window) (A0, A1)
struct CompositorWindow *ZuneCompositorFindWindow(struct LayerCompositor *comp, struct Window *window) (A0, A1)
void ZuneCompositorUpdate(struct LayerCompositor *comp) (A0)
void ZuneCompositorRefresh(struct LayerCompositor *comp) (A0)
void ZuneCompositorSetShadow(struct LayerCompositor *comp, WORD offsetX, WORD offsetY, UWORD blur, UBYTE alpha) (A0, D0, D1, D2, D3)

# Tag-based creation API
struct RenderContext *ZuneCreateRenderContextA(struct TagItem *tags) (A0)
struct DrawingBoard *ZuneCreateDrawingBoardA(struct TagItem *tags) (A0)
struct ZuneTexture *ZuneCreateTextureA(struct TagItem *tags) (A0)
struct LayerCompositor *ZuneCreateLayerCompositorA(struct TagItem *tags) (A0)
struct CompositorWindow *ZuneCompositorRegisterWindowA(struct TagItem *tags) (A0)

##end functionlist
